# 불변속성(Invariant)

모든 값은 여러가지 불변속성(Invariant)을 가지고 있을 수 있습니다. 아래 제시된 예시중 그 불변속성을 깨는 사례를 고르시오.

- [ ] 크기 20을 가지는 배열의 시작점을 가리키는 포인터에서 요소 4개의 크기만큼 전진한 것.
- [x] 인증된 유저를 표현하는 `AuthorizedUser` 타입의 값을 인증 절차 없이 생성하는 것.
- [ ] 0이 아닌 값을 표현하는 `NonZeroU8` 타입의 값을 포인터로 형변환 한 다음 그 값을 읽는 것.
