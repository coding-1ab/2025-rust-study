# for 반복문

아래 코드는 [1, 100] 범위 안의 짝수를 출력합니다. ___로 표기한 빈칸에 알맞은 코드를 고르시오.

```rs
fn main() {
    _________ {
        if i % 2 == 0 {
            println!("{i}");
        }
    }
}
```

- [ ] for(let mut i = 0; i < 100; i++)
- [ ] for(let mut i = 0; i <= 100; i++)
- [ ] for(let mut i : 1..100)
- [ ] for(let mut i : 1..=100)
- [x] for i in 1..=100
- [ ] for i in 1..100
- [ ] for(mut i : 1..100)
- [ ] for(mut i : 1..=100)
